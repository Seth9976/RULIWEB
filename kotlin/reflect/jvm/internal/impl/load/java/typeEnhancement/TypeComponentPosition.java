package kotlin.reflect.jvm.internal.impl.load.java.typeEnhancement;

public enum TypeComponentPosition {
    FLEXIBLE_LOWER,
    FLEXIBLE_UPPER,
    INFLEXIBLE;

    private static final TypeComponentPosition[] $values() [...] // Inlined contents
}

